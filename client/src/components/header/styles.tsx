import styled from 'styled-components';

export const Container = styled.header`
  width: 100%;
  height: 72px;
  background: rgba(32, 32, 36);
  box-shadow: 1px 3px 3px rgba(0,0,0,.25);
  position: fixed;
`;